export function lovefunc(flower1: number, flower2: number): boolean {
	return (flower1 + flower2) % 2 !== 0;
}

// an odd number plus an even number is always an odd number

// if total is an odd number, then it's true love
